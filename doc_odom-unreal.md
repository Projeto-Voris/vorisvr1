Websocket funcionando, a odometria está sendo lida pelo Unreal Engine e o peão está se movimentando de acordo com os dados fornecidos pelo SM2/pose.

Há de melhorar a relação entre os dados de rotação, pois o W não está sendo interpretado do modo correto (o ROV está com os dados Y e X de rotação invertidos, resultando em uma visualização distorcida)
